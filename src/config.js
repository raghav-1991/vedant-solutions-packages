/**
 * App-wide configuration.
 */

/** Brand accent, also mirrored as the CSS variable --brand in styles. */
export const BRAND = "#ff4f00";

/** Company / brand name shown on the page. */
export const BRAND_NAME = "Vedant Solutions";
